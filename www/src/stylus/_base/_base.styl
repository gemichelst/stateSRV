*
  box-sizing border-box
body
  height 100%
  font-family $font
  color $my-black
  font-size 16px
  // opacity 0
  // filter grayscale(1)
  // transition opacity 3s ease-in-out,filter 1s ease-in 3s
img
  max-width 100%
.container
  center($container)
strong 
  font-weight: bold
h1
h2
h3
h4
h5
h6
p 
  line-height 1.2em
section 
  padding 0
  margin 0
  min-height 100vh
  min-width 100vw
  max-width 100vw
  header 
    h2 
      color black
      font-weight: bold
      font-size: 32px
